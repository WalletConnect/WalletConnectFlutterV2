import 'package:flutter/material.dart';

class PageData {
  Widget page;
  String title;
  IconData icon;

  PageData({
    required this.page,
    required this.title,
    required this.icon,
  });
}
